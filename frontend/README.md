# Naptech Factory OS Frontend

Next.js 15 dashboard for the Naptech Factory OS MVP.

## Run Locally

```bash
cd frontend
npm install
cp .env.example .env.local
npm run dev
```

The app expects the FastAPI backend at `NEXT_PUBLIC_API_BASE_URL`.

